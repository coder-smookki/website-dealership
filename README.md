# SMK Dealership - Система управления автосалоном

Полнофункциональная веб-платформа для управления автосалоном премиум-класса с системой модерации объявлений, управлением заявками и административной панелью.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Функциональность](#функциональность)
- [Установка и запуск](#установка-и-запуск)
  - [Локальная разработка](#локальная-разработка)
  - [Docker](#docker)
- [Настройка окружения](#настройка-окружения)
- [Инициализация базы данных](#инициализация-базы-данных)
- [API Документация](#api-документация)
- [Роли пользователей](#роли-пользователей)
- [Система модерации](#система-модерации)
- [Разработка](#разработка)
  - [Линтеры и форматирование](#линтеры-и-форматирование)
  - [Структура кода](#структура-кода)
- [Деплой](#деплой)
- [Troubleshooting](#troubleshooting)

## 🚗 Описание проекта

SMK Dealership - это современная платформа для управления автосалоном, которая позволяет:

- **Владельцам** создавать и управлять объявлениями о продаже автомобилей
- **Администраторам** модерировать объявления, управлять заявками и пользователями
- **Покупателям** просматривать каталог автомобилей, фильтровать по параметрам и оставлять заявки

### Основные возможности

- ✅ Система модерации объявлений (pending → approved/rejected)
- ✅ Ролевая модель доступа (admin, owner)
- ✅ Управление статусами автомобилей (в продаже, забронирован, в архиве)
- ✅ Фильтрация автомобилей по множеству параметров
- ✅ Галерея изображений с зумом и навигацией
- ✅ Интеграция с Яндекс.Картами
- ✅ Административная панель с аналитикой
- ✅ Система заявок от покупателей
- ✅ Минималистичный современный дизайн

## 🛠 Технологический стек

### Backend
- **Fastify** - высокопроизводительный веб-фреймворк
- **MongoDB + Mongoose** - база данных и ODM
- **JWT** - аутентификация и авторизация
- **Swagger/OpenAPI** - документация API
- **Zod** - валидация данных
- **TypeScript** - типизированный JavaScript

### Frontend
- **React 18** - UI библиотека
- **Vite** - сборщик и dev-сервер
- **TypeScript** - типизация
- **React Router** - маршрутизация
- **Zustand** - управление состоянием
- **Axios** - HTTP клиент
- **Яндекс.Карты** - интеграция карт

### Инфраструктура
- **Docker & Docker Compose** - контейнеризация
- **Nginx** - веб-сервер для production
- **ESLint + Prettier** - линтинг и форматирование

## 📁 Структура проекта

```
car-shop/
├── apps/
│   ├── api/                    # Backend API
│   │   ├── src/
│   │   │   ├── config/        # Конфигурация (env, db)
│   │   │   ├── controllers/    # Контроллеры (обработчики запросов)
│   │   │   ├── models/         # Mongoose модели
│   │   │   ├── routes/         # Маршруты API
│   │   │   ├── services/       # Бизнес-логика
│   │   │   ├── middlewares/    # Middleware (auth, roles)
│   │   │   ├── plugins/        # Fastify плагины
│   │   │   ├── utils/          # Утилиты (валидация, ошибки)
│   │   │   ├── scripts/        # Скрипты (seed, create-admin)
│   │   │   ├── data/           # JSON данные для seed
│   │   │   └── server.ts       # Точка входа
│   │   ├── Dockerfile
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   └── web/                    # Frontend React
│       ├── src/
│       │   ├── api/            # API клиенты
│       │   ├── components/     # React компоненты
│       │   │   ├── admin/      # Компоненты админки
│       │   │   ├── common/     # Общие компоненты
│       │   │   ├── owner/      # Компоненты владельца
│       │   │   └── public/     # Публичные компоненты
│       │   ├── pages/          # Страницы приложения
│       │   ├── store/          # Zustand stores
│       │   ├── styles/         # Глобальные стили
│       │   ├── App.tsx         # Главный компонент
│       │   └── main.tsx        # Точка входа
│       ├── public/             # Статические файлы
│       ├── Dockerfile
│       ├── nginx.conf
│       ├── package.json
│       └── vite.config.ts
│
├── docker-compose.yml          # Docker Compose конфигурация
├── .dockerignore
├── .eslintrc.json              # ESLint конфигурация (root)
├── .prettierrc                 # Prettier конфигурация
├── .prettierignore
├── .env.example                # Пример переменных окружения
├── package.json                # Root package.json (workspaces)
└── README.md
```

## 🎯 Функциональность

### Публичная часть
- Каталог автомобилей с фильтрацией
- Детальная страница автомобиля
- Галерея изображений с зумом
- Страница контактов с картой
- Форма заявки на автомобиль

### Владелец (Owner)
- Регистрация и авторизация
- Создание объявлений (отправка на модерацию)
- Просмотр своих объявлений
- Управление статусами (в продаже, забронирован, в архиве)
- Фильтрация по статусам
- Статистика объявлений

### Администратор (Admin)
- Авторизация
- Модерация объявлений (одобрение/отклонение)
- Управление всеми объявлениями
- Управление заявками
- Управление пользователями
- Настройки сайта
- Создание объявлений (автоматически одобренные)

## 🚀 Установка и запуск

### Требования

- **Node.js** 18+ и npm
- **MongoDB** 5.0+ (локально или удаленно)
- **Docker & Docker Compose** (опционально, для контейнеризации)

### Локальная разработка

#### 1. Клонирование и установка зависимостей

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd website-car

# Установите зависимости
npm install
```

#### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# API Configuration
API_PORT=3001
API_HOST=0.0.0.0
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/car-shop

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
```

Создайте `.env.local` в `apps/web/`:

```env
VITE_API_URL=http://localhost:3001
```

#### 3. Запуск MongoDB

**macOS (Homebrew):**
```bash
brew services start mongodb-community
```

**Linux:**
```bash
sudo systemctl start mongod
```

**Windows:**
```bash
# Запустите MongoDB из установленной директории
mongod
```

#### 4. Инициализация базы данных

```bash
cd apps/api

# Создать администратора
npm run create-admin [email] [password]
# Пример: npm run create-admin admin@example.com admin123

# Заполнить базу данных (50 автомобилей, 5 владельцев)
npm run seed
```

#### 5. Запуск приложения

**Вариант 1: Одновременный запуск (рекомендуется)**

```bash
# Из корня проекта
npm run dev
```

**Вариант 2: Раздельный запуск**

Терминал 1 (API):
```bash
cd apps/api
npm run dev
```

Терминал 2 (Web):
```bash
cd apps/web
npm run dev
```

#### 6. Доступ к приложению

- **Frontend**: http://localhost:3000
- **API**: http://localhost:3001
- **Swagger UI**: http://localhost:3001/docs
- **Health Check**: http://localhost:3001/health

### Docker

#### Быстрый старт с Docker

```bash
# Создайте .env файл (см. выше)

# Запустите все сервисы
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Остановка с удалением volumes
docker-compose down -v
```

#### Сборка образов

```bash
# Сборка всех образов
docker-compose build

# Сборка конкретного сервиса
docker-compose build api
docker-compose build web
```

#### Инициализация базы данных в Docker

```bash
# Зайти в контейнер API
docker-compose exec api sh

# Внутри контейнера
npm run create-admin admin@example.com admin123
npm run seed
```

#### Переменные окружения для Docker

Docker Compose использует переменные из `.env` файла. Убедитесь, что:

```env
MONGODB_URI=mongodb://mongodb:27017/car-shop
JWT_SECRET=your-secret-key-change-in-production
CORS_ORIGIN=http://localhost:3000
```

## ⚙️ Настройка окружения

### Переменные окружения API (`apps/api/.env` или корневой `.env`)

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `API_PORT` | Порт API сервера | `3001` |
| `API_HOST` | Хост API сервера | `0.0.0.0` |
| `NODE_ENV` | Окружение (development/production) | `development` |
| `MONGODB_URI` | URI подключения к MongoDB | `mongodb://localhost:27017/car-shop` |
| `JWT_SECRET` | Секретный ключ для JWT | `change-me-in-production` |
| `JWT_EXPIRES_IN` | Время жизни JWT токена | `7d` |
| `CORS_ORIGIN` | Разрешенный origin для CORS | `http://localhost:3000` |

### Переменные окружения Web (`apps/web/.env.local`)

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `VITE_API_URL` | URL API сервера | `http://localhost:3001` |

## 🗄 Инициализация базы данных

### Создание администратора

```bash
cd apps/api
npm run create-admin [email] [password]
```

**Примеры:**
```bash
# С дефолтными значениями
npm run create-admin

# С кастомными значениями
npm run create-admin admin@smk-dealership.ru MySecurePassword123
```

**Дефолтные значения:**
- Email: `admin@example.com`
- Password: `admin123`

### Заполнение базы данных

```bash
cd apps/api

# Заполнить базу (50 автомобилей, 5 владельцев)
npm run seed

# Очистить и заполнить заново
npm run seed:clear
```

**Что создается:**
- 5 владельцев с email `owner1@car-shop.ru` ... `owner5@car-shop.ru`
- Пароль для всех владельцев: `owner123`
- 50+ автомобилей различных марок
- Настройки сайта (если их еще нет)

### Обновление настроек

```bash
cd apps/api
npm run update-settings
```

## 📚 API Документация

### Swagger UI

После запуска API, интерактивная документация доступна по адресу:
- **http://localhost:3001/docs**

### Основные эндпойнты

#### Public (публичные)

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/cars` | Список автомобилей (только одобренные) |
| GET | `/api/cars/:id` | Детали автомобиля |
| GET | `/api/settings` | Настройки сайта |
| POST | `/api/leads` | Создать заявку на автомобиль |
| GET | `/health` | Health check |

#### Auth (аутентификация)

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/auth/login` | Вход (admin/owner) |
| POST | `/api/auth/register` | Регистрация владельца |
| GET | `/api/auth/me` | Текущий пользователь |

#### Owner (владелец)

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/my/cars` | Мои объявления |
| GET | `/api/my/cars/:id` | Детали моего объявления |
| POST | `/api/my/cars` | Создать объявление (статус: pending) |
| PATCH | `/api/my/cars/:id/status` | Изменить статус (available/reserved/sold) |

#### Admin (администратор)

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/admin/cars` | Все объявления |
| POST | `/api/admin/cars` | Создать объявление (статус: approved) |
| PATCH | `/api/admin/cars/:id` | Обновить объявление |
| DELETE | `/api/admin/cars/:id` | Удалить объявление |
| PATCH | `/api/admin/cars/:id/moderate` | Модерировать объявление |
| GET | `/api/admin/leads` | Все заявки |
| PATCH | `/api/admin/leads/:id/status` | Изменить статус заявки |
| GET | `/api/admin/users` | Все пользователи |
| POST | `/api/admin/users` | Создать пользователя |
| GET | `/api/admin/settings` | Настройки сайта |
| PATCH | `/api/admin/settings` | Обновить настройки |

### Фильтры для `/api/cars`

| Параметр | Тип | Описание |
|----------|-----|----------|
| `page` | number | Номер страницы |
| `limit` | number | Количество на странице |
| `status` | string | Статус (available/reserved/sold) |
| `brand` | string | Бренд (case-insensitive) |
| `yearFrom` | number | Год от |
| `yearTo` | number | Год до |
| `priceFrom` | number | Цена от |
| `priceTo` | number | Цена до |
| `fuelType` | string | Тип топлива |
| `transmission` | string | КПП |
| `drive` | string | Привод |
| `q` | string | Поиск по названию/бренду/модели |
| `sort` | string | Сортировка (priceAsc, priceDesc, yearDesc) |

## 👥 Роли пользователей

### Администратор (admin)

**Доступ:**
- Полное управление объявлениями (создание, редактирование, удаление)
- Модерация объявлений владельцев
- Управление заявками от покупателей
- Управление пользователями (создание владельцев)
- Настройки сайта
- Просмотр аналитики

**Вход:** `/admin/login`

### Владелец (owner)

**Доступ:**
- Создание объявлений (отправка на модерацию)
- Просмотр и управление своими объявлениями
- Изменение статусов своих автомобилей
- Просмотр статистики своих объявлений

**Регистрация:** `/account/register`  
**Вход:** `/account/login`

## 🔍 Система модерации

### Процесс модерации

1. **Владелец создает объявление** → статус `pending`
2. **Администратор видит объявление** на странице `/admin/moderation`
3. **Администратор принимает решение:**
   - ✅ **Одобрить** → статус `approved`, объявление появляется на сайте
   - ❌ **Отклонить** → статус `rejected`, можно указать причину

### Статусы модерации

- `pending` - на модерации (по умолчанию для владельцев)
- `approved` - одобрено (видно на сайте)
- `rejected` - отклонено (не видно на сайте)

### Статусы продажи

- `available` - в продаже
- `reserved` - забронирован
- `sold` - продан (автоматически в архиве)

## 💻 Разработка

### Линтеры и форматирование

Проект использует ESLint и Prettier для поддержания качества кода.

**Проверка кода:**
```bash
# API
cd apps/api
npm run lint

# Web
cd apps/web
npm run lint
```

**Автоматическое исправление:**
```bash
# API
cd apps/api
npm run lint:fix

# Web
cd apps/web
npm run lint:fix
```

**Форматирование:**
```bash
# API
cd apps/api
npm run format

# Web
cd apps/web
npm run format
```

### Структура кода

#### Backend (API)

- **Controllers** - обработка HTTP запросов, валидация входных данных
- **Services** - бизнес-логика, работа с базой данных
- **Models** - Mongoose схемы
- **Routes** - определение маршрутов
- **Middlewares** - аутентификация, авторизация, проверка прав
- **Plugins** - Fastify плагины (CORS, JWT, DB, Logger, Swagger)
- **Utils** - утилиты (валидация Zod, обработка ошибок)

#### Frontend (Web)

- **Pages** - страницы приложения (public, owner, admin)
- **Components** - переиспользуемые компоненты
- **API** - клиенты для работы с API
- **Store** - Zustand stores для управления состоянием
- **Styles** - глобальные стили

### Стиль кода

- **TypeScript** - строгая типизация
- **ESLint** - проверка качества кода
- **Prettier** - автоматическое форматирование
- **Минималистичный дизайн** - черно-белая палитра

## 🚢 Деплой

### Production сборка

```bash
# Сборка API
cd apps/api
npm run build

# Сборка Web
cd apps/web
npm run build
```

### Docker Production

```bash
# Сборка образов
docker-compose build

# Запуск в production режиме
NODE_ENV=production docker-compose up -d
```

### Переменные окружения для Production

**Важно:** Измените следующие переменные:

```env
NODE_ENV=production
JWT_SECRET=<strong-random-secret>
MONGODB_URI=mongodb://your-production-mongodb:27017/car-shop
CORS_ORIGIN=https://your-domain.com
```

## 🐛 Troubleshooting

### Проблема: CORS ошибки

**Решение:**
1. Проверьте `CORS_ORIGIN` в `.env`
2. Убедитесь, что CORS плагин зарегистрирован первым в `server.ts`
3. Проверьте заголовки в Network tab браузера

### Проблема: JWT ошибки

**Решение:**
1. Проверьте `JWT_SECRET` в `.env`
2. Убедитесь, что JWT плагин зарегистрирован
3. Проверьте токен в localStorage браузера

### Проблема: MongoDB подключение

**Решение:**
1. Проверьте, что MongoDB запущен: `mongosh` или `mongo`
2. Проверьте `MONGODB_URI` в `.env`
3. Для Docker: используйте `mongodb://mongodb:27017/car-shop`

### Проблема: Изображения не загружаются

**Решение:**
1. Убедитесь, что файлы находятся в `apps/web/public/`
2. Используйте пути вида `/image.png` (начинается с `/`)
3. Проверьте консоль браузера на ошибки 404

### Проблема: Объявления не появляются на сайте

**Решение:**
1. Проверьте статус модерации (должен быть `approved`)
2. Проверьте статус продажи (должен быть `available`)
3. Убедитесь, что фильтры не скрывают объявления

## 📝 Скрипты

### Root (корень проекта)

```bash
# Разработка
npm run dev          # Запуск API и Web одновременно
npm run dev:api      # Запуск только API
npm run dev:web      # Запуск только Web

# Сборка
npm run build        # Сборка всех приложений
npm run build:api    # Сборка API
npm run build:web    # Сборка Web

# Линтинг и форматирование
npm run lint         # Проверить код во всех приложениях
npm run lint:fix     # Исправить ошибки линтера
npm run format       # Форматировать код

# Docker
npm run docker:build    # Собрать Docker образы
npm run docker:up        # Запустить контейнеры
npm run docker:down     # Остановить контейнеры
npm run docker:logs     # Просмотр логов
npm run docker:restart  # Перезапустить контейнеры
```

### API (`apps/api`)

```bash
npm run dev                    # Запуск в режиме разработки
npm run build                  # Сборка TypeScript
npm run start                  # Запуск production версии
npm run create-admin           # Создать администратора
npm run seed                   # Заполнить базу данных
npm run seed:clear             # Очистить и заполнить заново
npm run update-settings        # Обновить настройки сайта
npm run lint                   # Проверить код
npm run lint:fix               # Исправить ошибки линтера
npm run format                 # Форматировать код
```

### Web (`apps/web`)

```bash
npm run dev                    # Запуск dev-сервера
npm run build                   # Сборка для production
npm run preview                 # Предпросмотр production сборки
npm run lint                    # Проверить код
npm run lint:fix                # Исправить ошибки линтера
npm run format                  # Форматировать код
```

## 📄 Лицензия

Проект создан для SMK Dealership.

## 👨‍💻 Автор

smokkkkiiii Ilin Kirill
---

**Примечание:** Для production использования обязательно измените все секретные ключи и настройте безопасность согласно best practices.
